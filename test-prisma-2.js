const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function test() { try { const member = await prisma.companyMember.findFirst({ where: { status: 'ACTIVE' } }); console.log('Company:', member.companyId); const logs = await prisma.callLog.findMany({ where: { companyId: member.companyId } }); console.log('Logs:', logs); const agg = await prisma.callLog.aggregate({ where: { companyId: member.companyId }, _sum: { creditsUsed: true } }); console.log('Agg:', agg); } catch(e) { console.error('Error:', e); } } test().then(() => process.exit(0));
