const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function test() { try { const member = await prisma.companyMember.findFirst({ where: { status: 'ACTIVE' } }); console.log('Member:', member.companyId); console.log('Testing aiAgent...'); const activeAgents = await prisma.aiAgent.count({ where: { companyId: member.companyId, status: 'ACTIVE' } }); console.log('activeAgents:', activeAgents); } catch(e) { console.error('aiAgent error:', e); } } test().then(() => process.exit(0));
